// Casualty StepHeader simply re-uses the Property header form for now.
// This keeps folder parity with `steps/property` and allows us to add
// Casualty-specific tweaks later without touching the Property code.
import PropertyHeader from '../property/StepHeader';

export default PropertyHeader;
