echo "➡️ Generating docs before commit..."
npm run --silent generate:docs || {
  echo "Docs generation failed. Aborting commit."; exit 1;
}

# Optionally add generated files if not already tracked
git add docs/*.generated.json docs/GENERATED.md 2>/dev/null || true

echo "✅ Docs updated. Proceeding with commit."

